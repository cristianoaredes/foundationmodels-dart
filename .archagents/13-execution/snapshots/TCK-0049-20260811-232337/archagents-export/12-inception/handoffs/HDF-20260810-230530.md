# HDF — session handoff 2026-08-10T23:05:30-03:00

**Snapshot:** `.archagents/13-execution/snapshots/TCK-0017-20260811-020511`
**Resume:** `/ops-continue` (resume-over-restore if tree dirty)

## Estado

Backlog drain + parity push substantially complete (pure-Dart + ios-bridge U1–U8).
Residuals: TCK-0004/0016 blocked (device), TCK-0017 open (publish mirror).

## Contratos críticos

1. **Tools single-executor:** `autoExecuteTools` default true; `FmAgent` uses false.
2. **Swift link:** `FOUNDATIONMODELS_SWIFT_PATH` → monorepo `swift/`.
3. **Parity honesty:** zero Flutter `supported` without on-device smoke.

## Próximo passo

- Device available → TCK-0016 smokes + flip parity cells
- Or publish path → TCK-0017 vendored foundationmodels-swift
