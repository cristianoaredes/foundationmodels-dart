name: foundationmodels_server
description: OpenAI-compatible HTTP server for foundationmodels-dart (phase 8).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  foundationmodels:
    path: ../foundationmodels
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
  http: ^1.2.0
