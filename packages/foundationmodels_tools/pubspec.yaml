name: foundationmodels_tools
description: Tool-calling helpers for foundationmodels-dart (phase 4).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  foundationmodels:
    path: ../foundationmodels

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
