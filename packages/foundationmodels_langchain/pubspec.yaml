name: foundationmodels_langchain
description: LangChain.dart-compatible adapter for foundationmodels (phase 8).
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.0

dependencies:
  foundationmodels:
    path: ../foundationmodels

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
