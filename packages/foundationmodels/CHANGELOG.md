# Changelog

## 0.1.0

- Initial workspace release candidate (git-only / ADR-0002).
- Pure-Dart API: respond, stream, sessions, tools duplex, guided schema, mock provider.
- Depends on `foundationmodels_platform_interface` (path in monorepo; hosted at publish time).
