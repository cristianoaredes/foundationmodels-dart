name: foundationmodels_platform_interface
description: >-
  Platform interface contracts for the FoundationModels Dart/Flutter adapter:
  transport envelope, stream events, typed errors and models mirroring the
  upstream foundationmodels-js protocol (v2). Pure Dart, no Flutter.
version: 0.1.0
publish_to: none
homepage: https://github.com/cristianoaredes/foundationmodels-dart
repository: https://github.com/cristianoaredes/foundationmodels-dart
issue_tracker: https://github.com/cristianoaredes/foundationmodels-dart/issues

environment:
  sdk: ^3.12.0

resolution: workspace

dev_dependencies:
  test: ^1.24.0
  lints: ^5.0.0
