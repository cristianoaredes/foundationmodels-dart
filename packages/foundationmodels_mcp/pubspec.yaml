name: foundationmodels_mcp
description: >-
  Minimal MCP (Model Context Protocol) stdio server over FoundationModels —
  tools/list + tools/call + fm_respond. Not a replacement for foundationmodels_agent.
version: 0.1.0
publish_to: none
resolution: workspace
homepage: https://github.com/cristianoaredes/foundationmodels-dart
repository: https://github.com/cristianoaredes/foundationmodels-dart

environment:
  sdk: ^3.12.0

dependencies:
  foundationmodels:
    path: ../foundationmodels

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
