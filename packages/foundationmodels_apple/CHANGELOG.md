# Changelog

## 0.0.1

- Initial workspace release candidate (git-only / ADR-0002).
- Flutter plugin (iOS + macOS) bridging platform channels to FoundationModelsIOSBridge.
- Default SPM pin: foundationmodels-swift `from: "1.0.4"`.
